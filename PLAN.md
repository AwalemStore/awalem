# خطة تطوير مشروع عوالم (Awalem)

> تاريخ الإعداد: أبريل 2026
> حالة المشروع: قيد التطوير

---

## المرحلة 0: إنشاء مستودع GitHub

| # | المهمة | التفصيل | الحالة |
|---|--------|---------|--------|
| 0 | تهيئة Git | `git init` في مجلد المشروع | ⬜ |
| 1 | إنشاء `.gitignore` | استبعاد `.DS_Store` وملفات النظام | ⬜ |
| 2 | إنشاء مستودع GitHub | باستخدام `gh repo create` | ⬜ |
| 3 | رفع المشروع الحالي | كـ initial commit | ⬜ |

---

## المرحلة 1: إصلاحات أمنية عاجلة

| # | المهمة | التفصيل | الحالة |
|---|--------|---------|--------|
| 4 | إصلاح نظام الدخول | الاعتماد الحالي على `localStorage` فقط يسمح لأي شخص بالدخول عبر Console. **الحل:** إضافة تحقق سيرفر-جانبي عند كل تحميل صفحة (Token أو Session) | ⬜ |
| 5 | حماية XSS | المدخلات المخصصة (اسم العاملة، المهام، القوانين) تُدرج مباشرة في HTML بدون تنقيح. **الحل:** إضافة دالة `escapeHTML()` وتطبيقها على كل المدخلات قبل إدراجها في DOM | ⬜ |
| 6 | إخفاء روابط API | روابط Google Apps Script مكشوفة في الكود. **الحل:** نقلها لملف `config.js` منفصل أو تشفيرها | ⬜ |

---

## المرحلة 2: تنظيم الكود

| # | المهمة | التفصيل | الحالة |
|---|--------|---------|--------|
| 7 | إعادة تسمية ملف | ملف `<!DOCTYPE html>.html` في قاموس الأطعمه اسمه غير صالح. **الحل:** إعادة تسميته لـ `index.html` | ⬜ |
| 8 | استخلاص نظام الدخول | كود تسجيل الدخول مكرر 3 مرات بنفس المنطق. **الحل:** استخلاصه في ملف `auth.js` مشترك | ⬜ |
| 9 | فصل الملفات | كل HTML ملف واحد ضخم يحتوي CSS + JS + بيانات. **الحل:** فصل كل صفحة لـ `style.css` + `app.js` + `data.js` (للقواميس) | ⬜ |
| 10 | توحيد رموز اللغات | القاموس العام يستخدم `or` للأورومو، بينما الروتين يستخدم `om`. **الحل:** توحيد الرموز عبر كل الملفات | ⬜ |

---

## المرحلة 3: تحسينات تجربة المستخدم (UX)

| # | المهمة | التفصيل | الحالة |
|---|--------|---------|--------|
| 11 | شريط بحث | القواميس فيها 200-250 كلمة بدون خاصية بحث. **الحل:** إضافة شريط بحث يفلتر الكلمات فورياً حسب النص العربي أو المترجم | ⬜ |
| 12 | حفظ تلقائي | المهام والقوانين المختارة تضيع لو المستخدم أغلق الصفحة. **الحل:** حفظ الاختيارات في `localStorage` تلقائياً واستعادتها عند فتح الصفحة | ⬜ |
| 13 | الوضع الداكن | التطبيقات الثلاثة بدون Dark Mode. **الحل:** إضافة تبديل بين الوضع الفاتح والداكن مع حفظ التفضيل | ⬜ |
| 14 | تصدير كصورة | حالياً فقط طباعة/PDF. **الحل:** إضافة زر تصدير PNG/JPG باستخدام `html2canvas` (محمل بالفعل) | ⬜ |
| 15 | تحويل لـ PWA | التطبيقات تعمل فقط متصل. **الحل:** إضافة `manifest.json` + `service worker` لتعمل بدون إنترنت وتتثبت كتطبيق على الجوال | ⬜ |

---

## المرحلة 4: تحسينات الأداء

| # | المهمة | التفصيل | الحالة |
|---|--------|---------|--------|
| 16 | ترجمة مسبقة | استخدام MyMemory API للترجمة اللحظية بطيء ومحدود. **الحل:** ترجمة المهام والقوانين مسبقاً وتخزينها في الكود (زي القواميس) | ⬜ |
| 17 | تحميل تدريجي | كل الـ 250 كلمة تُعرض مرة واحدة. **الحل:** عرض حسب الفئة مع إمكانية التوسيع، أو تحميل تدريجي عند التمرير | ⬜ |
| 18 | ضغط الصور | ملفات PNG و JPG كبيرة. **الحل:** ضغطها لتسريع التحميل مع الحفاظ على الجودة | ⬜ |

---

## المرحلة 5: ميزات جديدة

| # | المهمة | التفصيل | الحالة |
|---|--------|---------|--------|
| 19 | صفحة رئيسية | إنشاء صفحة `index.html` رئيسية تجمع الأدوات الثلاثة (الروتين الذكي + القاموس العام + قاموس الأطعمه) بتصميم موحد | ⬜ |
| 20 | تقييم الأداء الأسبوعي | إضافة نظام تتبع بسيط لإنجاز المهام مع إحصائيات أسبوعية للمستخدم | ⬜ |

---

## ملاحظات عامة

- كل مرحلة تعتمد على اللي قبلها
- يتم عمل commit بعد كل مهمة منتهية
- يتم اختبار كل تعديل على الجوال والديسكتوب قبل الانتقال للمهمة التالية

---

> صنع من قبل عوالم Awalem
